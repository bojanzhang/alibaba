# dataId 默认使用spring.application.name 配置跟文件扩展名结合（配置格式默认使用 properties）
# GROUP 不配置 默认使用DEFAULT_GROUP
#spring.application.name=nacos-config
spring.cloud.nacos.config.server-addr=127.0.0.1:8848
# 使用yaml 作为配置   显示声明 dataId 的文件扩展名类型
##spring.cloud.nacos.config.file-extension=yaml
##spring.application.name=nacos-config-yaml
### spring.cloud.nacos.config.refresh.enabled=false  可关闭动态刷新
###在没有明确指定 ${spring.cloud.nacos.config.namespace} 配置的情况下，
### 默认使用的是 Nacos 上 Public 这个namespae。如果需要使用自定义的命名空间，可以通过以下配置来实现：
### spring.cloud.nacos.config.namespace=XXX(nacos 配置命名空间id)